initSidebarItems({"enum":[["Error","Enumeration of errors which can occur while encoding a rust value into a TOML value."]],"fn":[["encode","Encodes an encodable value into a TOML value.This function expects the type given to represent a TOML table in some form. If encoding encounters an error, then this function will fail the task."],["encode_str","Encodes an encodable value into a TOML string.This function expects the type given to represent a TOML table in some form. If encoding encounters an error, then this function will fail the task."]],"struct":[["Encoder","A structure to transform Rust values into TOML values.This encoder implements the serialization `Encoder` interface, allowing `Encodable` rust types to be fed into the encoder. The output of this encoder is a TOML `Table` structure. The resulting TOML can be stringified if necessary.Example"]]});